#!/bin/bash

cat <<EOF

DOMAIN=${DOMAIN}
GO_SERVICE_PORT=${GO_SERVICE_PORT}
ANGULAR_UI_PORT=${ANGULAR_UI_PORT}
ADMIN_PASSWORD=${ADMIN_PASSWORD}
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID} 
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET} 

APP_HOST=${APP_HOST}
TLS_DIR=${TLS_DIR}
APP_CERT=${APP_CERT_NAME}
APP_PORT=${APP_PORT}
APP_CERT=${APP_CERT}
APP_KEY=${APP_KEY}

EOF

